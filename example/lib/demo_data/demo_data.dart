/// The rows every part of the example runs on.
///
/// This lived inside the playground until the recipes needed it. A recipe may
/// import Flutter, this package, and this library — and nothing else, because a
/// recipe is something a reader copies into their own app. Data reachable only
/// through the playground would have made that rule impossible to keep.
library;

export 'employee.dart';
export 'random_data_generator.dart';
